/** Quiet status shown while a response is streaming. */
export const THINKING_STATUS_TEXT = 'Thinking...';

/** Hide the duration footer for sub-second noise and very short replies. */
export const MIN_VISIBLE_RESPONSE_SECONDS = 5;
